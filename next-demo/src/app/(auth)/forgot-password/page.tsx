// Put the folder name in "()" make it to be omitted from the URL, called Route Group
// auth -> http://localhost:3000/auth/forgot-password
// (auth) -> http://localhost:3000/forgot-password

export default function ForgotPassword() {
    return <h1>Forgot password</h1>
};
